# Netlify deploy config — Naseeha homepage (Vite + React static build).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page site: serve index.html for any path.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
